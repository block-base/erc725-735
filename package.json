{
  "scripts": {
    "compile": "truffle compile",
    "deploy": "rm -rf build; truffle migrate --reset --network rinkeby"
  },
  "dependencies": {
    "ethereumjs-wallet": "^0.6.2",
    "fs": "0.0.1-security",
    "truffle-hdwallet-provider": "^0.0.6",
    "truffle-privatekey-provider": "^0.1.0",
    "web3": "^1.0.0-beta.36",
    "web3-utils": "^1.0.0-beta.36"
  }
}
